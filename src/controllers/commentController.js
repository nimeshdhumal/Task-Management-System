const commentService = require('../services/commentService');

module.exports = {

       getSingleCommnet: async(req,res) => {
            try {
                
            } catch (error) {
                
            }
       }

}